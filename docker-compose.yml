services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: langguessr
      MYSQL_DATABASE: langguessr
    ports:
      - "3306:3306"
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql